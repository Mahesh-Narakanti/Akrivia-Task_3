export interface Message {
  username: string;
  msg: string;
    color: string;
    id: string;
}
export interface User{
  username: string;
  status: string;
  color: string;
    id: string;
    notification: number;
}